# Prebuilt image from Docker Hub. Override IMAGE for a local build.
IMAGE=docker.io/ylongwang/sony-camera-inbox-organizer:latest
WEB_PORT=18088
PUID=1000
PGID=1000
TZ=Asia/Shanghai

# Host paths mounted into the container as /config and /data.
CONFIG_DIR=./runtime/config
MEDIA_ROOT=./runtime/data
